{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "profile",
      "type": "shell",
      "command": "go test -c dynamic -o ./profile/world.test && ./profile/world.test -test.run=NONE -test.benchmem -test.bench=World -test.cpuprofile=./profile/world.cpu.log dynamic && go tool pprof -text -nodecount=10 ./profile/world.test ./profile/world.cpu.log",
      "group": {
        "kind": "test",
        "isDefault": true
      },
      "options": {
        "env": {
          "GOPATH": "${workspaceRoot}"
        }
      }
    }
  ]
}